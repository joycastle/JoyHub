// Keep this shell free of CSS transforms: fixed search bars inside discovery pages must
// be positioned against the viewport rather than against an animated ancestor.
export const APP_SHELL_PAGE_CLASS_NAME = 'space-y-8'
